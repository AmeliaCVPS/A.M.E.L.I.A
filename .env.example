# Database (PostgreSQL)
# Required for both frontend and backend
DATABASE_URL=postgresql://user:password@localhost:5432/amelia

# Session secret (random string for production)
SESSION_SECRET=your-super-secret-session-key

# Node environment
NODE_ENV=development

# Frontend port (dev only)
PORT=5173

# Base URL for the frontend
BASE_URL=/
