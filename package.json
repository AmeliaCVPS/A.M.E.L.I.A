{
  "name": "amelia-totem",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --filter @workspace/amelia-totem --filter @workspace/api-server --parallel run dev",
    "build": "pnpm -r --if-present run build",
    "build:frontend": "pnpm -r --filter @workspace/amelia-totem run build",
    "build:backend": "pnpm -r --filter @workspace/api-server run build",
    "typecheck": "tsc --build",
    "lint": "pnpm -r --if-present run lint"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  },
  "packageManager": "pnpm@10.0.0"
}
